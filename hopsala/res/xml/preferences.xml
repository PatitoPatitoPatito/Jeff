<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:defaultValue="false"
        android:key="debug"
        android:summary="Enabling this feature makes the application less user friendly."
        android:title="Debug Mode" />
    <MultiSelectListPreference
        android:entries="@array/color_array"
        android:entryValues="@array/color_values"
        android:key="colors"
        android:summary="Select which colors are sought after during autonomous operation"
        android:title="Color Selection" />

</PreferenceScreen>